Explain the purpose of comments in HTML and provide an example of how to use comments in an HTML document?

The comment tag is used to insert comments in the source code. we can use comments to explain our code.comments are not renderd on browser.
which can help you when you edit the source code at a later date. this is especially useful if we have a lot of code.


Write a short note on tag and element with an example?

TAG

Tags are used to mark up the start of an HTML element and they are usually enclosed in angle brackets. 

An example of a tag is: <h1>.

ELEMENT

An element usually consists of a StartTag & Endtag.

<h1> Hello There </h1>


What is the DOCTYPE Declaration in HTML?

The DOCTYPE declaration is an instruction to the web browser about what version of HTML the page is written in.










